# Logging em desenvolvimento
logging.level.br.jus.tjpi.agendatelefonica=DEBUG
logging.level.org.springframework.ldap=DEBUG

# Importar propriedades base
spring.config.import=optional:file:.env[.properties]

# Sobrescrever CORS para permitir o frontend local no modo dev
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173